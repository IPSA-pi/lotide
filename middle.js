// Psudo code for middle.js

// crear variable middle []
// checar si arr.length es par o non
// si es non
  // middleIndex = Math.floor(arr.length / 2)
  // middle.push(arr[middleIndex])
// si es par
  // middleIndexStart = (arr.length / 2) - 1
  // middle.push(arr.slice(middleIndexStart, middleIndexStart + 2))
// return middle